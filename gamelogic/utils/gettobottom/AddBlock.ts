import GameStatus from "../../status";
import CalcPiles from "./CalcPiles";

const AddBlock = () => {
    // GameStatus.field.field[GameStatus.field.heightBlock - CalcPiles()[GameStatus.block.positionX / GameStatus.block.size] - 1][GameStatus.block.positionX / GameStatus.block.size] = GameStatus.block.id;
}

export default AddBlock;